
version: 2

models:
  - name: core_coins
    description: "Core coins table"
    columns:
      - name: coin_id
        description: "Unique ID for each coin"
        tests:
          - not_null
          - unique
